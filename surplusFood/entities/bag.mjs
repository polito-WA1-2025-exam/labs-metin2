export default function Bag(id, type, status, price, size, establishmentId, food_items) {
  this.id = id;
  this.type = type;
  this.status = status;
  this.price = price;
  this.size = size;
  this.establishmentId = establishmentId;
  this.food_items = food_items;
}
